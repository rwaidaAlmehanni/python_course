#modules are libraries on python that have built_in function we can import the module and user the function on this library
import time
print time.asctime() #asctime() is a function on time module that return the current time on the system as string